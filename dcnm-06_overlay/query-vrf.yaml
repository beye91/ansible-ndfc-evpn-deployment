---

- hosts: dcnm_controllers
  gather_facts: false

  vars:
    fabric: cbeye_cml

  tasks:
    - name: Query all VRFs deployed within the fabric
      cisco.dcnm.dcnm_vrf:
        fabric: "{{ fabric }}"
        state: query